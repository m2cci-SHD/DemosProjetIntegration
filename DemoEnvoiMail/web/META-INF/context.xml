<?xml version="1.0" encoding="UTF-8"?>
<Context antiJARLocking="true" path="/DemoEnvoiMail">
  <!-- 
       cela serait mieux de procéder avec une ressource gérée par le serveur
       mais cela impose d'avoir le droit de copier la fichier javax.mail.jar dans
       le lib du serveur tomcat. Mais sur les machines de l'ufr les étudiants n'ont
       pas les droits administrateur et écrire ce fichier dans le répertoire nblib
       de .netbeans ne suffit pas.
       Du coup la ressource est configurée dans le fichier web.xml, comme paramètres
       d'initalisation de la servlet TicketsCtrler
    -->
  <!--
    <Resource 
        name="mail/DEMO"
        type="javax.mail.Session"
        auth="Container" 
        mail.smtp.auth="true"
        mail.smtp.starttls.enable="true"
        mail.smtp.host="smtps.univ-grenoble-alpes.fr"
        mail.smtp.port="587"
        mail.smtp.user="VotreLoginUGA"
        mail.smtp.password="VotreMotDePasseUGA"
        password="VotreMotDePasseUGA"
    />
    -->
</Context>
